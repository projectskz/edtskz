# Конструктор бизнес-процессов (1С)

Тиражируемое **расширение** 1С (префикс `кбп_`) для ERP, КА, УТ, УНФ, БП (УХ — релиз не определён).
Платформа 8.3.27 (8.5 — по остаточному принципу). Разработка в Конфигураторе, **без EDT**; исходники — XML-выгрузка расширения.

Документы (источник истины, читать по необходимости, целиком не загружать):
- `docs/ТЗ.md` — требования ФТ-xxx/НФ-xx, этапы 0–8, минимальные релизы (п. 1.4), принятые решения (п. 7).
- `docs/Архитектура.md` — решения АР-1..9, модель данных (п. 4), движок (п. 6), адресация (п. 7), кнопки (п. 8), оповещения (п. 9), API (п. 10).
- `docs/АнализПрототипа.md` — разбор KPI 3.1 (Волгасофт): что берём (И-x) и чего избегаем (С-x).
- `docs/СпецификацияМетаданных.md` — состав объектов расширения; **машиночитаемая**: `python3 tools/gen/generate.py` строит из неё XML в `src/cfe/`. Структуру меняем в спецификации, потом генерируем.

Ключевые решения:
- Свой движок на маркерах; платформенные БП/Задачи не используем.
- Редактор — `ГрафическаяСхема`, только картинка; **все настройки в справочниках и регистрах** в разрезе версии схемы; опубликованная версия неизменна; синхронизация схемы↔справочник автоматическая по неизменяемому `ИдЭлемента`.
- Процесс — справочник `кбп_Процессы`, переменные — ТЧ процесса (ПВХ `кбп_ПеременныеПроцессов`). Замещение — документ `кбп_Замещение`.
- Отличия конфигураций — только в `кбп_АдаптерКонфигурации`; формы типовых не заимствуем.
- До этапа 6 — только зарегистрированные обработчики, без произвольного кода. Лицензирование — этап 7; Фреш, мобильный, интеграции, 8.5 — этап 8.

Код вендора (KPI) в репозиторий не коммитить.

Текущий шаг: первая загрузка `src/cfe/` в эталонную базу (чек-лист — п. 15 спецификации), затем прототип редактора схем (этап 0).
