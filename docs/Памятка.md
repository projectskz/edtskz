# Памятка разработчика (состояние и грабли)

## Где мы

- Этап 0 проверен. Этап 1 загружается и работает до создания задачи. Проверены конструктор, панель свойств, запуск и задача.
- Этап 2 (маршруты) собран: [Этап2.md](Этап2.md). Код платформой не проверялся.
- Пользователь проходит **сквозной сценарий** [Тестирование.md](Тестирование.md): этапы 1 и 2, шаги пронумерованы. Ошибки исправляем по номерам шагов.
- Ветка: `claude/friendly-hopper-nd0j4n`. PR не создаём без запроса.
- Пользователю отдаём **полный архив** `src/cfe` без `ConfigDumpInfo.xml`, с `Configuration.xml` в корне (python `zipfile`, UTF-8 имена). Он загружает его целиком в расширение `КонструкторБизнесПроцессов`.
- Пожелание пользователя: **в формах — акцент на удобство** (сводки, подсказки, компактная вёрстка, минимум лишних кликов).

## Как устроен код

| Что | Где |
|---|---|
| Метаданные (справочники, регистры, перечисления, роли) | `docs/СпецификацияМетаданных.md` → `python3 tools/gen/generate.py` → `build/cfe` (для частичной загрузки) |
| Формы и обработки | `tools/forms/build.py` (описание на Python) + `tools/forms/formlib.py` → сразу в `src/cfe` |
| Модули (общие и форм) | правятся прямо в `src/cfe/**/Module.bsl`, формат: UTF-8 с BOM, CRLF |
| Движок | `кбп_Движок` (запуск, задачи, выбор ветки, возврат, прерывание, журнал) |
| Кнопки и обработчики | `кбп_Действия`; адресация — `кбп_Адресация`; почта — `кбп_Оповещения` + `кбп_АдаптерКонфигурации` |
| Конструктор: синхронизация схемы, проверка, публикация, панель свойств | `кбп_КомпиляторСхем` |
| Условия-отборы (СКД): схема полей, вычисление, представление | `кбп_Условия` |
| Очередь действий: таймеры, продолжение родителя после вложенного | `кбп_Движок.ОбработатьОчередьДействий` (регламентное + фоновое после операции) |

`build.py <каталог .bsl>` дополнительно перезаписывает модули из каталога исходников. **Без аргумента** он пишет только XML форм. Модули сейчас правим в `src/cfe`, поэтому запускаем без аргумента.

## Проверки без платформы (скрипты в `tools/check`)

- **Порядок тегов форм.** Сравнение с 635 формами БСП (`SSL=<клон ssl_3_1> python3 tools/check/formcheck.py <Form.xml>`). Канонический порядок уже зашит в `formlib.ORDER`, и сборщик падает на неизвестном свойстве.
- **BSL (`python3 tools/check/bslcheck.py src/cfe`)** проверяет:
  - парность блоков;
  - существование и экспорт вызываемых методов наших модулей;
  - число аргументов;
  - обработчики событий, команд и оповещений;
  - `Элементы.X` против формы;
  - директивы компиляции;
  - имена процедур, совпадающие с ключевыми словами.
- **Метаданные.** Значения перечислений в коде сверяются с `Enums/*.xml`, поля в запросах — с реквизитами объектов.
- **Эталоны формата.** БСП `1c-syntax/ssl_3_1` (sparse-checkout в scratchpad/ssl). Имена стандартных команд ищем через `mcp__github__search_code`, у него лимит запросов.

## Грабли (уже наступили — не повторять)

1. `Новый X().Свойство` — ошибка компиляции. Сначала присвоить объект переменной.
2. **Ключевые слова нельзя давать процедурам**, в том числе обработчикам команд формы: `Прервать`, `Продолжить`, `Возврат`, `Выполнить`, `Новый` и т. д. Поэтому `ПрерватьПроцесс`, `ПродолжитьПроцесс`. Значение перечисления `кбп_ВидыДействий.Прервать` через точку допустимо.
3. Переменные с именами глобальных функций или модулей (`Строка`, `Пользователи`, `Параметры`) не использовать.
4. **`ExtendedPresentation`** есть только у метаданных форм обработок и отчётов. У форм справочников Конфигуратор его отвергает.
5. Колонки таблицы формы, привязанной к ТЧ объекта: `DataPath` = `Объект.ТЧ.Колонка`, а не `ТЧ.Колонка`.
6. Длина номера строки ТЧ — 5 при режиме совместимости ниже 8.3.27.
7. Формы задачи и процесса при создании нового элемента: `Отказ = Истина` с сообщением, а не исключение.
8. Палитра поля графической схемы задаётся стандартными командами `Form.Item.<Поле>.StandardCommand.InsertItem<Вид>`.
   - Подтверждены: `Start`, `Activity`, `Condition`, `Processing`, `Completion`, `Decoration`.
   - Команды для соединительной линии нет: линии тянутся мышью.
   - Для этапа 2 ещё не найдены имена команд для выбора варианта, разделения, слияния и вложенного процесса.
9. `ТекущийЭлемент` поля схемы недоступен в веб-клиенте. Там элемент выбирается в списке шагов справа.
10. Заимствованный `Справочник.Пользователи` сопоставляется по именам, один `.cfe` на все конфигурации. UUID в XML базы разработки: `fa1ef4b3-…`.
11. `ЗаполнитьЗначенияСвойств(Приёмник, Источник, , "Исключения")`: исключаемые свойства должны **существовать в источнике**, иначе ошибка «Поле … не обнаружено». Лишнее не исключать — перезаписать после.
12. Элементы схемы неизвестного вида и декорации пропускаются: у части из них `Имя` только для чтения.
13. **Python и CRLF.** `open(p).read()` без `newline=""` превращает CRLF в LF, и замена многострочного текста не находится, а запись теряет CRLF. Правки модулей — через `tools/bslpatch.py` (блоки `@@@ OLD` / `@@@ NEW` / `@@@ END`) или с чтением байтов (`open(p,"rb")`). После правок: `file …/Module.bsl` должен показывать «with BOM, with CRLF».
14. **Продолжение родителя после вложенного процесса — только через очередь** (`ПродолжитьРодителя`). Если продолжать сразу, контекст родителя может быть уже открыт выше по стеку: дубли номеров журнала, потеря переменных.
15. Каталог `Ext/Form/` для модуля новой формы `build.py` не создаёт: модуль кладётся отдельно (`os.makedirs`).
16. Параметры формы (`Параметры.X`) нужны только в `ПриСозданииНаСервере`: то, что понадобится позже, копировать в реквизиты формы.

## Модель кнопок и веток

- **Кнопки шага:**
  - если настроены в `кбп_КнопкиШагов`, используются они;
  - если линий из задачи несколько и на них нет условий, каждая линия становится кнопкой с `ИдКнопки = ИдПерехода`;
  - иначе одна кнопка «Выполнено».

  При `РазрешенВозврат` добавляется системная кнопка «Вернуть на доработку» (`кбп_Возврат`).
- **Выбор ветки** (`ВыбратьПереход`) выполняется в таком порядке:
  1. линия кнопки;
  2. «Результат задачи» (`ИдКнопки` линии);
  3. для Условия и Выбора варианта — подпись линии или название варианта совпадает с заголовком нажатой кнопки;
  4. условие (обработчик или отбор СКД);
  5. «По умолчанию».
- **Ветки.**
  - Разделение: маркер ждёт (`ОжидаетсяВеток`), дочерние маркеры получают `РодительскийМаркер`.
  - Слияние: `ПришлоВеток` растёт; когда набралось нужное число, остальные ветки закрываются (`ЗакрытьВетки`), родительский маркер идёт дальше.
  - Завершение закрывает все маркеры процесса.
- **Возврат в ветках:** `ЛинияМаркера` — сам маркер и его предки. Возврат на шаг предка закрывает ветки предка.
- **«Каждому»:** шаг ждёт всех исполнителей. Кнопка вида «Опасная» завершает шаг сразу.

## Этап 3 — в работе (не закончен, в архив пользователю ещё не отдавался)

**Сделано и закоммичено:**
- **Движок:**
  - `ПереадресоватьЗадачу`, `ПередатьЗадачу`, `ВыполнитьЗадачуВКонтексте` (общая для ручного и автовыполнения);
  - `ЗапуститьПроцессИзЗадачи`, `КонтекстЗадачи`;
  - контроль срока через очередь (`КонтрольСрока` → просрочка, эскалация или `Автовыполнение`);
  - `ЗадачиПользователя` с замещаемыми (колонка `ЗаКого`), `ЗавершитьЗамещения`;
  - автозапуск (`ПоставитьАвтозапуск`, `ВыполнитьАвтозапуск`, вид очереди `ЗапускПоРасписанию`), `ЗапуститьПоРасписанию`;
  - `ПроцессыПредмета`, `ЗадачиПредметаПользователя`.
- **`кбп_Адресация`:** `ЗамещаемыеПользователем` (цепочки), `ЗаместителиПользователя`, `ЗамещениеДляЗадачи`, `ИсполнительЭскалации`.
- **`кбп_Действия`:** в `КнопкиЗадачи` — условие видимости и системная кнопка «Переадресовать…»; `КнопкаВидна`, `КнопкаАвтовыполнения`.
- **`кбп_Оповещения`:** оповещения заместителям, `ОповеститьОЗамещении`, текст письма «Задача просрочена».
- **`кбп_ПроцессыСлужебный`:** обработчики подписок `ПриЗаписиОбъекта` и `ОбработкаПроведенияДокумента`, `АвтозапускРазрешен`, `ПравилаПоРасписанию`, `ОбновитьПравилаЗапуска`. `кбп_ПроцессыПовтИсп.ТипыСПравиламиЗапуска`.
- **`кбп_КомпиляторСхем`:** условия видимости кнопок сохраняются при записи шага; `УсловиеВидимостиКнопки` / `ЗаписатьУсловиеВидимостиКнопки`; при публикации пересчитываются правила запуска; новые проверки (просрочка без срока, параметры кнопок).
- **Документ `кбп_Замещение`:** модуль объекта (проверки, запрет циклов, движения, оповещение).
- **Регламентное задание** `КонтрольСроков` (включается адаптером, 15 мин).
- **Формы в `build.py`:** `ПравилаЗапуска`, `ПроцессыПредмета`, `Замещение.ФормаДокумента`; в конструкторе — просрочка, переадресация, колонки кнопок и команды «Условие видимости…» и «Правила запуска»; в задаче — колонки `Действие` и `ИмяФормы` в таблице `Кнопки`; в «Моих задачах» — колонка «За кого».
- **Модули форм:** `ПравилаЗапуска`, `ПроцессыПредмета`.

**Осталось:**
1. Модуль `Замещение.ФормаДокумента`: `ПриСозданииНаСервере`, `ОбластьДействияПриИзменении` — видимость таблиц «Роли» и «Схемы».
2. Модуль конструктора:
   - команды `УсловиеВидимости` (открыть `УсловиеПерехода`, запись через `ЗаписатьУсловиеВидимостиКнопки`) и `ПравилаЗапуска`;
   - список выбора `ЭлементШагаКнопкаАвтовыполнения` (кнопки шага);
   - видимость групп «Просрочка» и «Автовыполнение» в `НастроитьПанель` (только для задачи);
   - заполнение `ТекстВидимости` в `ЗагрузитьШаг`.
3. `УсловиеПерехода`: параметр `ЗаголовокФормы`.
4. Модуль задачи:
   - заполнить `Действие` и `ИмяФормы` в `СоздатьКнопки`;
   - на клиенте: «Переадресовать» (`ПоказатьВводЗначения` пользователя + комментарий → `кбп_ПроцессыAPI.Переадресовать`), «Открыть форму» («Предмет» или имя формы; два сегмента → дописать `.Форма`), «Запустить вложенный процесс» (`кбп_Движок.ЗапуститьПроцессИзЗадачи`);
   - в сводке — «за кого».
5. «Мои задачи»: `ЗапуститьОбработкуОчереди` при обновлении; колонка «За кого» видна, если есть. `ЗапускПроцесса`: при переданном `Предмет` — отбор схем по типу, автовыбор единственной.
6. **Метаданные** (писать в `build.py`):
   - `EventSubscriptions/кбп_ПриЗаписиОбъекта`: `cfg:DocumentObject` + `cfg:CatalogObject`, `OnWrite` → `кбп_ПроцессыСлужебный.ПриЗаписиОбъекта`;
   - `EventSubscriptions/кбп_ПриПроведенииДокумента`: `Posting` → `ОбработкаПроведенияДокумента`;
   - `CommonCommands/кбп_ПроцессыПредмета` (`FormNavigationPanelGoTo`) и `кбп_ЗапуститьПроцессПоПредмету` (`FormCommandBarImportant`), параметр `cfg:DocumentRef` + `cfg:CatalogRef`, `Ext/CommandModule.bsl`. Образец — БСП `CommonCommands/ЗадачиПоПредмету.xml`;
   - `Configuration.xml`: `EventSubscription` после `CommonModule`, `CommonCommand` после `ScheduledJob`;
   - права `View` на команды в ролях.
7. `bslcheck`: добавить `ObjectModule` и `CommandModule`. Прогнать `bslcheck` и `formcheck`.
8. Документация: `Этап3.md`, часть 3 в `Тестирование.md`, архив пользователю.

## Дальше

1. Исправлять ошибки по [Тестирование.md](Тестирование.md). Получить выгрузку расширения из Конфигуратора в `src/cfe`.
2. Этап 3 (люди):
   - замещение (`кбп_Замещение`);
   - эскалация и контроль сроков (`КонтрольСроков`);
   - переадресация;
   - правила запуска (при записи или проведении, по расписанию);
   - программируемые кнопки (условия видимости);
   - панель процесса в формах объектов.
3. Долги этапа 2:
   - множественный запуск вложенных процессов (ФТ-703);
   - возврат «в родителя» (ФТ-707);
   - поля из реквизитов предмета.
4. Открытые вопросы: веб-клиент; подключение `.cfe` к БП/УНФ.
